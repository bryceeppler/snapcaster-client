export type Website = {
  name: string;
  slug: string;
  url: string;
  code: string;
  discount:number;
  tcgs: string[];
  image_source: string;
};

export type WebsiteMapping = {
  name: string;
  slug: string;
};
