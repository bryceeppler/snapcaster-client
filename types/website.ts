export type Website = {
  name: string;
  code: string;
  url: string;
  shopify: boolean;
  backend: string;
  image: string;
  promoCode: string;
  discount: number;
};

export type WebsiteMapping = {
  name: string;
  code: string;
};
